# shellcheck shell=sh

# This file is part of a workflow that uses the 1Password CLI (https://1password.com/downloads/command-line/)
# and Environment Variables to safely inject Secret Zero-type data by polling a project-specific 1Password Vault.
#
# For more information, see the README.md file.

# GitHub-specific configuration
export GITHUB_TOKEN="op://Shared/github/tokens/scorecard-cli"
